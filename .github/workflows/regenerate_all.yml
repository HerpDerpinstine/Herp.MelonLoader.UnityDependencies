name: Regenerate All

on:
  workflow_dispatch:

concurrency:
  group: generate
  cancel-in-progress: true

jobs:
  fetchdeps:
    name: Fetch Dependencies
    uses: ./.github/workflows/runner.yml
    secrets: inherit
    permissions:
      contents: write
    with:
      UPDATE_EXISTING: true
